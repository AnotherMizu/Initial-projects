userGuess = document.getElementById("userGuess")
startBoton = document.getElementById("startBoton")

startBoton.addEventListener("click", function(){
    
})

function random_number(min, max) {
    min = Math.ceil(min);
    max = Math.floor(max);
    return Math.floor(Math.random() * (max - min + 1) + min);
}

function game() {

}